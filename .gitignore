# dependencies
/node_modules
/.pnp
.pnp.*

# builds
/.next/
/out/
/dist/
/build/

# testing
/coverage

# misc
.DS_Store
*.pem
*.tsbuildinfo
next-env.d.ts

# debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env*

# Private research material and any real site data. The original .ipa, extracted
# binaries, real configuration archives, security keys and LAN addresses live
# here and must never be committed.
/work/

# Local runtime state (saved sites, cached user profiles).
/config/
